  {
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "eu-west-1",
    "source_ami": "ami-d2414e38",
    "instance_type": "t2.micro",
    "ssh_username": "ubuntu",
    "ami_name": "devopsloft {{timestamp}}"
  }],
  "provisioners": [
  {
    "type": "shell",
    "script": "provision.sh"
  }
  ]
}
